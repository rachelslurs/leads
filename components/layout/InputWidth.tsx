const InputWidth = ({ children }: { children: React.ReactNode }) => {
    return (
        <div className="max-w-md w-full mx-auto">
            {children}
        </div>
    )
}

export default InputWidth;